const express = require('express');
var controllerTemplate = require('../controllers/main');

var router = express.Router();

// router.get("/", controllerTemplate.viewTemplates)
// router.post("/select", controllerTemplate.select);



module.exports = router;