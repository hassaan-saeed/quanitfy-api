const express = require('express');
var controllerFile = require('../controllers/main');

var router = express.Router();

// router.get("/", controllerFile.viewFile);
// router.get("/:_id", controllerFile.viewoneFile);
// router.post("/save", controllerFile.save);
// router.delete('/:id', controllerFile.delete);


module.exports = router;